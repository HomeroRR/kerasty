//! Shared building blocks: the core enums ([`definitions`]) and the [`Layer`] /
//! [`Model`] traits ([`traits`]) that the rest of the crate is built on.
//!
//! [`Layer`]: crate::Layer
//! [`Model`]: crate::Model

pub mod definitions;
pub mod traits;
